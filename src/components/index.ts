import ComponentsPlugin from './components-plugin';

/**
 * List public declaration here.
 */
export { default as ComponentsPlugin, ComponentPluginOptions } from './components-plugin';

// Preserve the default import to prevent breaking changes when using legacy import.
export default ComponentsPlugin;
